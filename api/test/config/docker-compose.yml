version: '3.8'
services:
  mongodb:
    image: mongo:latest
    container_name: api-test-instance
    ports:
      - '27018:27017' # Map host port 27017 to container port 27018
    # volumes: # Can be used for data persistence later
    # environment:
    # MONGO_INITDB_ROOT_USERNAME: admin
    # MONGO_INITDB_ROOT_PASSWORD: password
